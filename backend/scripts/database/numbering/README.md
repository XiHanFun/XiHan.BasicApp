# 业务编号数据库脚本

业务编号表在运行时仍由项目现有的 SqlSugar CodeFirst 初始化流程创建。这里的脚本用于 DBA 审阅、受控发布和显式回滚，不会改变应用版本号，也不会被自动升级流程主动执行。

支持的数据库目录：

- `postgresql`
- `mysql`
- `sqlserver`
- `sqlite`
- `oracle`

每个目录包含 `up.sql` 与 `down.sql`。执行正向脚本前应确认目标库尚不存在同名表；执行回滚脚本会永久删除业务编号规则和发号审计记录，必须先停止发号并完成备份。
