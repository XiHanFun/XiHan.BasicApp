export enum EnableStatus {
  Disabled = 0,
  Enabled = 1,
}

export enum ValidityStatus {
  Invalid = 0,
  Valid = 1,
}

export enum PermissionType {
  ResourceBased = 0,
  Functional = 1,
  DataScope = 2,
}
